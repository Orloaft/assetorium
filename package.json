{
  "name": "asset-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "No-code visual tool to slice source images into game-ready sprite sheets, tilesets, and stage maps with collisions. Exports portable PNG + JSON.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "vite preview --port 4173 & sleep 3 && URL=http://localhost:4173/ node smoke.mjs; kill %1 2>/dev/null"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
